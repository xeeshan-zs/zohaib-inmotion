import { ArrowLeft, ArrowUpRight } from 'lucide-react';
import { ColorCompare, VideoPlayer } from '@/app/components';
import { defaultProfile, sampleProjects } from '@/lib/content';
import NotFound from './not-found';
export default function ProjectPage({id}:{id:string}){const profile=defaultProfile;const projects=sampleProjects.filter(p=>p.published);const p=projects.find(p=>p.id===id);if(!p)return <NotFound/>;const next=projects[(projects.findIndex(v=>v.id===id)+1)%projects.length];return <main className="project-page"><header className="site-header"><a className="wordmark" href="/">{profile.name.toUpperCase()}<span>✳</span></a><a className="underlined-link" href="/#work"><ArrowLeft size={16}/> All work</a></header><section className="project-hero"><div className="eyebrow">{p.category.toUpperCase()} / {p.year} {p.sample&&' / SAMPLE CONCEPT'}</div><h1>{p.title}</h1><p>{p.summary}</p></section><div className="project-feature">{p.video?<VideoPlayer url={p.video} title={p.title} poster={p.cover} captions={p.captions}/>:<img src={p.cover} alt={p.title}/>}</div><section className="case-study"><div className="case-facts"><div><span className="eyebrow">ROLE / FOCUS</span><p>{p.role}</p></div><div><span className="eyebrow">FORMAT</span><p>{p.category} {p.duration&&`/ ${p.duration}`}</p></div><div><span className="eyebrow">YEAR</span><p>{p.year}</p></div></div><div className="case-story"><span className="eyebrow">THE THINKING BEHIND IT</span><h2>{p.sample?'A DIRECTION.':'BEHIND THE CUT.'}</h2><p>{p.approach}</p>{p.credits&&<div className="credits"><span className="eyebrow">CREDITS & CONTEXT</span><p>{p.credits}</p></div>}{!p.video&&<p className="fine-print">{p.sample?'This project is an illustrative concept, presented as a still-image study.':'The film will be added here soon.'}</p>}</div></section>{p.before&&p.after&&<section className="project-grade"><h2>BEFORE / AFTER</h2><ColorCompare before={p.before} after={p.after}/></section>}{projects.length>1&&<a href={`/work/${next.id}`} className="next-project"><span className="eyebrow">NEXT PROJECT</span><h2>{next.title}<ArrowUpRight/></h2></a>}<footer className="project-bottom"><a href={`mailto:${profile.email}`}>Say hello <ArrowUpRight size={18}/></a><a href="/#work">Back to all work</a></footer></main>}
